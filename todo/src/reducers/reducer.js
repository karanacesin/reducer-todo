export const initialState = {
    item: '',
    completed: false,
    id: new Date(),
  };
  
  export const reducer = (state, action) => {
    switch (action.type) {
      
      default:
        return state;
    }
  };